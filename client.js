
// ? Client just implements this API

export const ContentClient = {
  fetch (operation, options) {
    // ? operation is some MUI specific pojo
    // ? options are MUI Provider configuration options

    // ? returns Promise or Observable
  },

  store (operation, options) {
    // ? operation is some MUI specific pojo
    // ? options are MUI Provider configuration options

    // ? returns Promise or Observable
  }

  // ? An API for checking if in Authoring Mode?
}
