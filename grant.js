
const DialogProps = {
  title: 'string',
  paragraphText: 'string',
  buttonText: 'string',
  date: 'date'
}
const Dialog = (
  <Dialog />
)
const DialogLarge = new MuiComponent(Dialog, DialogSchema)
const DialogLoose = new MuiComponent(Dialog)
